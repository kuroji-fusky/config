import { Config } from "tailwindcss"

export default {
  content: [],
  theme: {
    extend: {
      spacing: { unset: "unset" },
      colors: {
        current: "currentColor"
      }
    }
  }
} satisfies Config
